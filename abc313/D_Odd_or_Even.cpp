#include<bits/stdc++.h>

using namespace std;
const int MAXN = 2e5 + 5;
int n, m, a[MAXN];
string s;
int main(){
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  cin >> n >> m;
  int x;
  cout << "? ";
  for (int i = 1; i <= m; i++){
    cout << i << ' ';
  }
  cout << endl;
  while (cin >> x){
    if (x == -1){
      return 0;
    }
    
  }
  return 0;
}